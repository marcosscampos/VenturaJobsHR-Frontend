// import * as admin from 'firebase-admin'

export default ({app, route}) => {

  let user = {
    email: 'marcosvinicius.analucia@gmail.com',
    password: '123456789'
  }

  // admin.auth().getUserByEmail(user.email).then(response => {
  //   console.log(response.email)
  // })

  // auth.currentUser.getIdTokenResult().then(response => {
  //   if(response.claims.role === 'company') {
  //
  //   }
  // })
}
